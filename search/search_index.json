{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"","title":"Software de Conectividad","text":"<p>En este repositorio encontrar\u00e1 guias para la configuracion de algunos servicios relacionados al software de conectividad en las redes.</p>"},{"location":"#requisitos","title":"Requisitos","text":"<ul> <li>Sistema operativo CentOS 7.1 (version Minimal-2009). Puede instalarlo en una maquina virtual con VirtualBox o en una maquina fisica.</li> <li>Ejecutar la configuracion inicial para instalar las herramientas necesarias para seguir los tutoriales.</li> </ul>"},{"location":"#guias","title":"Guias","text":"<ul> <li>Despliegue de una pagina web en HTML</li> </ul>"},{"location":"#anexos","title":"Anexos","text":"<ul> <li>Instalacion de CentOS 7.1 en VirtualBox</li> <li>Conexion a internet de una maquina virtual CentOS 7.1</li> </ul>"},{"location":"services/apache-server/","title":"Configuracion de un servidor Apache","text":"<p>Apache es un software de servidor web de c\u00f3digo abierto que se utiliza para alojar sitios web en Internet. Es uno de los servidores web m\u00e1s populares y se utiliza en una gran cantidad de sitios web en todo el mundo.</p> <p>Proporciona una variedad de caracter\u00edsticas y funcionalidades para alojar sitios web, incluyendo soporte para lenguajes de programaci\u00f3n como PHP, Python y Perl, y una amplia variedad de m\u00f3dulos y complementos.</p>"},{"location":"services/apache-server/#instalacion-de-paquetes","title":"Instalacion de paquetes","text":"<p>Para instalar el servidor DNS (BIND) en CentOS 7.1, ejecute el siguiente comando:</p> <pre><code>yum -y install httpd php\n</code></pre>"},{"location":"services/apache-server/#configuracion-del-servicio-httpd","title":"Configuracion del servicio httpd","text":"<p>Primero vamos a configurar el archivo de configuracion principal del servidor httpd, el cual se encuentra en la ruta <code>/etc/httpd/conf/httpd.conf</code>.</p> <pre><code>vim /etc/httpd/conf/httpd.conf\n</code></pre> <p>En el archivo de configuracion, debemos agregar la siguiente configuracion:</p> <pre><code>ServerName apache.example.com\n</code></pre> <p>Donde apache.example.com va a ser el nombre de dominio donde va a estar alojado el sitio web.</p> <p>Una vez instalado el paquete, el servicio httpd se encuentra deshabilitado. Para habilitarlo e iniciarlo, ejecute el siguiente comando:</p> <pre><code>systemctl enable httpd.service\n</code></pre> <pre><code>systemctl start httpd.service\n</code></pre> <p>Tambien puede verificar el estado del servicio o detenerlo con los siguientes comandos:</p> <pre><code>systemctl status httpd.service\n</code></pre> <pre><code>systemctl stop httpd.service\n</code></pre> <p>Para verificar que el servicio este funcionando correctamente puede usar el siguiente comando:</p> <pre><code>netstat -tulpn | grep httpd\n</code></pre>"},{"location":"services/apache-server/#carga-del-sitio-web","title":"Carga del sitio web","text":"<p>Para cargar el sitio web en el servidor, debe copiar el contenido del sitio web en la ruta <code>/var/www/html</code>. Para ello, puede usar una conexion SFTP. Puedes usar el archivo index.html como ejemplo.</p>"},{"location":"services/apache-server/#acceso-al-sitio-web-desde-un-navegador","title":"Acceso al sitio web desde un navegador","text":"<p>Para acceder al sitio web desde un navegador, debe ingresar la direccion IP del servidor en la barra de direcciones del navegador. Adicionalmente, si tienes configurado un servicio DNS puedes usar el nombre de dominio configurado como <code>ServerName</code> en el servidor Apache.</p>"},{"location":"services/dns-configuration/","title":"Configuracion de un servidor DNS","text":"<p>Un servidor DNS (Domain Name System) es un servicio que se utiliza para traducir los nombres de dominio de sitios web en direcciones IP (Protocolo de Internet). Cuando un usuario intenta acceder a un sitio web, el navegador web env\u00eda una solicitud al servidor DNS para resolver el nombre de dominio en una direcci\u00f3n IP. Una vez que se ha resuelto la direcci\u00f3n IP, el navegador web puede conectar con el servidor web que aloja el sitio web y solicitar su contenido.</p> <p>Un servidor DNS es un componente esencial de Internet que permite que los nombres de dominio se traduzcan en direcciones IP para que los usuarios puedan acceder a los sitios web. Sin un servidor DNS, los usuarios tendr\u00edan que recordar y utilizar las direcciones IP para acceder a los sitios web, lo que ser\u00eda mucho m\u00e1s dif\u00edcil que simplemente escribir un nombre de dominio f\u00e1cil de recordar.</p>"},{"location":"services/dns-configuration/#instalacion-de-paquetes","title":"Instalacion de paquetes","text":"<p>Para instalar el servidor DNS (BIND) en CentOS 7.1, ejecute el siguiente comando:</p> <pre><code>yum -y install bind bind-chroot bind-utils bind-libs caching-nameserver\n</code></pre>"},{"location":"services/dns-configuration/#configuracion-de-archivos","title":"Configuracion de archivos","text":"<ul> <li>Archivo de configuracion /etc/named.conf</li> <li>options</li> <li>logging</li> <li>Zonas IPv4<ul> <li>Zona .</li> <li>Zona para IPv4</li> </ul> </li> <li>Zonas IPv6<ul> <li>Zonas IPv6</li> </ul> </li> <li>Archivo de zona directa</li> <li>example.com.zone</li> <li>Archivos de zonas inversas</li> <li>192.168.10.zone</li> <li>2001_db8_1_0.zone</li> </ul>"},{"location":"services/dns-configuration/#etcnamedconf","title":"/etc/named.conf","text":"<p>El archivo <code>/etc/named.conf</code> contiene la configuracion del servidor DNS. En este archivo se especifican las zonas de DNS que el servidor maneja, asi como las opciones de configuracion del servidor.</p> <p>Una muestra del archivo <code>/etc/named.conf</code> se encuentra en el siguiente archivo named.conf donde se ha usado <code>example.com</code> como nombre de dominio y <code>192.168.10.11</code> y <code>2001:db8:1::101</code> como direcciones IPv4 e IPv6 del servidor DNS, respectivamente.</p>"},{"location":"services/dns-configuration/#options","title":"options","text":"<p>Seccion para definir las rutas de ubicaci\u00f3n de los archivos de configuraci\u00f3n para cada una de las zonas.</p> <pre><code>options {\n  listen-on    port 53 { 127.0.0.1; 192.168.10.11; };\n  listen-on-v6 port 53 { ::1; 2001:db8:1::101; };\n\n  directory \"/var/named\";\n  dump-file \"/var/named/data/cache_dump.db\";\n  statistics-file \"/var/named/data/named_stats.txt\";\n  memstatistics-file \"/var/named/data/named_mem_stats.txt\";\n\n  allow-query     { any; };\n\n  recursion yes;\n  notify yes;\n\n  dnssec-enable yes;\n  dnssec-validation yes;\n  dnssec-lookaside auto;\n  bindkeys-file \"/etc/named.iscdlv.key\";\n  managed-keys-directory \"/var/named/dynamic\";\n\n  pid-file \"/run/named/named.pid\";\n  session-keyfile \"/run/named/session.key\";\n};\n</code></pre> <p>en este caso <code>192.168.10.11</code> y <code>2001:db8:1::101</code> son las IPv4 e IPv6 del servidor DNS, respectivamente.</p>"},{"location":"services/dns-configuration/#logging","title":"logging","text":"<p>Seccion para definir el nivel de log del servidor DNS.</p> <pre><code>logging {\n  channel default_debug {\n    file \"data/named.run\";\n    severity dynamic;\n  };\n};\n</code></pre>"},{"location":"services/dns-configuration/#zonas-ipv4","title":"Zonas IPv4","text":"<p>Los servidores DNS basan sus configuraciones en la definici\u00f3n de zonas o dominios, y estos deben ser definidos inicialmente en el fichero \u201cnamed.conf \u201d. Las siguientes configuraciones van a ser parte de dicho archivo:</p> <ul> <li>Zona <code>.</code></li> </ul> <pre><code>zone \".\" IN {\n  type hintl\n  file \"named.ca\";\n};\n\ninclude \"/etc/named.rfc1912.zones\";\ninclude \"/etc/named.root.key\";\n</code></pre> <ul> <li>Zona para IPv4</li> </ul> <p>En esta secci\u00f3n se definen las zonas para IPv4, tambi\u00e9n encontramos la definici\u00f3n para la resoluci\u00f3n de dominios y direcciones IP tanto directas como inversas.</p> <pre><code>zone \"example.com\" IN {\n    type master;\n    file \"example.com.zone\";\n};\n\nzone \"192.168.10.in-addr.arpa\" {\n  type master;\n  file \"192.168.10.zone\";\n};\n</code></pre> <p>donde se ha usado <code>example.com</code> como nombre de dominio y <code>192.168.10</code> como porcion de red de la IPv4 del servidor DNS.</p>"},{"location":"services/dns-configuration/#zonas-ipv6","title":"Zonas IPv6","text":"<p>En esta secci\u00f3n se detallan las zonas para IPv6 y encontramos la definici\u00f3n para la resoluci\u00f3n de dominios y direcciones IP tanto directas como inversas.</p> <ul> <li>Zona para IPv6</li> </ul> <pre><code>zone \"0.0.0.0.1.0.0.0.8.b.d.0.1.0.0.2.ip6.arpa\" {\n  type master;\n  file \"2001_db8_1_0.zone\";\n};\n</code></pre>"},{"location":"services/dns-configuration/#archivo-de-zonas-directas","title":"Archivo de zonas directas","text":"<ul> <li>Archivo /var/named/example.com.zone: El archivo \u201cexample.com.zone\u201d contiene la definici\u00f3n del servidor DNS para la zona directa definida en el direccionamiento para IPv4 y se indica mediante el registro NS.</li> </ul>"},{"location":"services/dns-configuration/#archivo-de-zonas-inversas","title":"Archivo de zonas inversas","text":"<ul> <li> <p>Archivo /var/named/192.168.10.zone: El archivo \u201c192.168.10.zone\u201d contiene la definici\u00f3n del servidor DNS para la zona inversa definida en el direccionamiento para IPv4 y se indica mediante el registro NS.</p> </li> <li> <p>Archivo /var/named/2001_db8_1_0.zone: El archivo \u201c2001_db8_1_0.zone\u201d contiene la definici\u00f3n del servidor DNS para la zona directa definida en el direccionamiento para IPv6 y se indica mediante el registro NS.</p> </li> </ul>"},{"location":"services/dns-configuration/#verificaciones-de-configuracion-de-archivos-de-zona","title":"Verificaciones de configuracion de archivos de zona","text":"<p>Para verificar la sintaxis de los archivos de zona, ejecute el siguiente comando:</p> <pre><code>named-checkzone example.com example.com.zone\n</code></pre> <pre><code>named-checkzone 192.168.10.zone\n</code></pre> <pre><code>named-checkzone 2001_db8_1_0.zone\n</code></pre>"},{"location":"services/dns-configuration/#seleccion-del-servidor-dns","title":"Seleccion del servidor DNS","text":"<p>Para seleccionar el servidor DNS, se debe editar el archivo <code>/etc/resolv.conf</code> y colocar la del servidor DNS.</p> <pre><code>vim /etc/resolv.conf\n</code></pre> <pre><code># Generated by NetworkManager\nsearch example.com\nnameserver 2001:db8:1::101\nnameserver 192.168.10.11\n</code></pre> <p></p>"},{"location":"services/dns-configuration/#verifcacion-del-servidor-dns","title":"Verifcacion del servidor DNS","text":"<p>Primero podemos reiniciar el servicio:</p> <pre><code>systemctl restart named\n</code></pre> <p>Para verificar que el servidor DNS esta desplegado adecuadamente, ejecute el siguiente comando:</p> <pre><code>netstat -tulpn | grep named\n</code></pre> <p>Para verificar el funcionamiento del servidor DNS, ejecute alguno de los siguientes comandos:</p> <pre><code>nslookup\n  &gt; server\n</code></pre> <p>Debe verse la configuracion del servidor DNS:</p> <p></p> <p>Tambien se pueden comprobar las diferentes consultas DNS con los siguientes comandos:</p> <pre><code>dig dns6.example.com AAAA\n</code></pre> <pre><code>dig MX example.com\n</code></pre> <pre><code>nslookup www.example.com\n</code></pre> <pre><code>nslookup www6.example.com\n</code></pre> <pre><code>nslookup pop6.example.com\n</code></pre> <pre><code>nslookup imap6.example.com\n</code></pre> <pre><code>nslookup smtp6.example.com\n</code></pre> <pre><code>nslookup streaming6.example.com\n</code></pre>"},{"location":"services/html-web-page/","title":"Creacion de una pagina web","text":"<p>Una forma sencilla de crear paginas web estaticas es usando HTML. HTML es un lenguaje de marcado de hipertexto. Es el lenguaje estandar para crear paginas web.</p> <p>La sintaxis basica de una pagina web hecha en HTML seria la siguiente:</p> <pre><code>&lt;!DOCTYPE html&gt;\n&lt;html&gt;\n  &lt;head&gt;\n    &lt;title&gt;Titulo de la pagina&lt;/title&gt;\n  &lt;/head&gt;\n  &lt;body&gt;\n    &lt;p&gt;Contenido de la pagina&lt;/p&gt;\n  &lt;/body&gt;\n&lt;/html&gt;\n</code></pre> <p>Puede encontrar mas informacion sobre HTML en el siguiente enlace: w3schools.com. Puede encontrar un ejemplo muy sencillo de una pagina web en el siguiente enlace: index.html.</p>"},{"location":"services/sftp/","title":"Establecer una conexion SFTP a un servidor","text":"<p>SFTP es un protocolo de transferencia de archivos seguro que proporciona una capa adicional de seguridad sobre el protocolo FTP. SFTP es un protocolo seguro que proporciona autenticaci\u00f3n y cifrado de datos.</p>"},{"location":"services/sftp/#instalacion","title":"Instalacion","text":"<p>Para establecer la conexion con un servidor SFTP en CentOS 7.1, necesita instalar el paquete openssh-server. Para instalar el paquete, ejecute el siguiente comando:</p> <pre><code>yum -y install openssh-server openssh-clients openssl\n</code></pre>"},{"location":"services/sftp/#verificacion-del-servicio-ssh","title":"Verificacion del servicio SSH","text":"<p>Una vez instalado el paquete, el servicio SSH se encuentra deshabilitado. Para habilitarlo e iniciarlo, ejecute el siguiente comando:</p> <pre><code>systemctl enable sshd.service\n</code></pre> <pre><code>systemctl start sshd.service\n</code></pre> <p>Para comprobar que el servicio este funcionando correctamente, puede usar el siguiente comando:</p> <pre><code>netstat -tulpn | grep sshd\n</code></pre> <p></p> <p>Por defecto el servicio SSH esta configurado para escuchar en el <code>port 22</code>.</p>"},{"location":"services/sftp/#instalacion-de-filezilla","title":"Instalacion de FileZilla","text":"<p>FileZilla es un cliente FTP y SFTP multiplataforma. Es un software de c\u00f3digo abierto y gratuito.</p>"},{"location":"services/sftp/#conexion-a-un-servidor-usando-sftp-en-filezilla","title":"Conexion a un servidor usando SFTP en FileZilla","text":"<p>Para establecer una conexion SFTP a un servidor, debe configurar los siguientes parametros:</p> <ul> <li>Host: Nombre de dominio o direccion IP del servidor.</li> <li>Port: Puerto del servidor. Por defecto es el puerto 22.</li> <li>Username: Nombre de usuario con el que se va a conectar al servidor.</li> <li>Password: Contrase\u00f1a del usuario.</li> </ul> <p></p>"},{"location":"tutorials/internet-connection/","title":"Index","text":""},{"location":"tutorials/internet-connection/#conexion-a-internet-de-una-maquina-virtual-en-centos-7","title":"Conexion a Internet de una maquina virtual en CentOS 7","text":"<p>Enciende tu marquina virtual e inicia sesi\u00f3n como root.</p> <p></p> <p>Desde aca, vamos al siguiente directorio:</p> <pre><code>  cd /etc/sysconfig/network-scripts\n</code></pre> <p>En este punto debemos ajustar la configuracion de la interfaz de la tarjeta de red que usaremos para conectarnos. En mi caso es la <code>enp0s3</code>.</p> <p>Primero vamos a hacer un backup de nuevo archivo <code>ifcfg-enp0s3</code>, ejecutando el siguiente comando:</p> <pre><code>  cp ifcfg-enp0s3 ifcfg-enp0s3._backup\n</code></pre> <p>Para verificar que el backup fue creado correctamente ejecutamos el siguiente comando:</p> <pre><code>  ls -lah | grep ifcfg-enp0s3\n</code></pre> <p></p> <p>Ahora que tenemos el backup, vamos a editar el archivo <code>ifcfg-enp0s3</code> ejecutando:</p> <pre><code>  vi ifcfg-enp0s3\n</code></pre> <p>Actualiza las siguientes lineas:</p> <pre><code>  ONBOOT=yes\n</code></pre> <p>Ahora, vamos a configurar la puerta de enlace en el archivo <code>/etc/sysconfig/network</code>. Para hacerlo ejecutamos:</p> <pre><code>  cd /etc/sysconfig\n</code></pre> <p>Nos aseguramos de hacer un backup de nuestro archivo <code>network</code>, ejecutando:</p> <pre><code>  cp network network_backup\n</code></pre> <p>Para verificar que el backup fue creado correctamente ejecutamos el siguiente comando:</p> <pre><code>  ls -lah | grep network\n</code></pre> <p></p> <p>Ahora agreamos algunas lineas al archivo <code>network</code>:</p> <pre><code>  vi network\n</code></pre> <p>Debe ser un archivo vacio, agregamos las siguientes lineas:</p> <pre><code>  NETWORKING=yes\n  HOSTNAME=centos7\n  GATEWAY=192.168.10.1\n</code></pre> <p>Deberia quedar coi algo como esto:</p> <p></p> <p>El valor de <code>GATEWAY</code> es la puerta de enlace de tu maquina host. En este caso, es <code>192.168.10.1</code>.</p> <p>Ahora vamos a configurar el name servers en el archivo <code>/etc/resolv.conf</code>. Para ir a la carpeta ejecutamos:</p> <pre><code>  cd /etc\n</code></pre> <p>Como siempre, hacemos un backup del archivo <code>resolv.conf</code> ejecutando:</p> <pre><code>  cp resolv.conf resolv.conf_backup\n</code></pre> <p>Para verificar que el backup fue creado correctamente ejecutamos el siguiente comando:</p> <pre><code>  ls -lah | grep resolv.conf\n</code></pre> <p></p> <p>Ahora agregamos algunas lineas al archivo <code>resolv.conf</code>:</p> <pre><code>  vi resolv.conf\n</code></pre> <p>Inicialmente debe ser un archivo vacio, agregamos las siguientes lineas:</p> <pre><code>  nameserver 8.8.8.8\n  nameserver 8.8.4.4\n</code></pre> <p>Antes de continuar tenemos que asegurarnos que nuestra maquina virtual tiene el adaptador de red configurado correctamente. En este caso estamos usando el adaptador NAT Network Adapter. Si tienes que configurarlo y tu maquina virtual esta encendida, tienes que reiniciarla.</p> <p></p> <p>Ya casi estamos. Nos aseguramos de reiniciar el servicio de red ejecutando:</p> <pre><code>  systemctl restart network\n</code></pre> <p>Ahora, vamos a probar nuestra conexion a internet ejecutando:</p> <pre><code>  ping google.com\n</code></pre> <p></p>"},{"location":"tutorials/internet-connection/#notas","title":"Notas","text":"<p>Si quieres saber cual es la ip de tu maquina virtual, ejecuta:</p> <pre><code>  ip addr\n</code></pre> <p></p>"},{"location":"tutorials/set-static-ip/","title":"Configurar una IP est\u00e1tica en CentOS 7.1","text":"<p>Enciende tu marquina virtual e inicia sesi\u00f3n como root.</p> <p></p> <p>Desde aca, vamos al siguiente directorio:</p> <pre><code>  cd /etc/sysconfig/network-scripts\n</code></pre> <p>En este punto debemos ajustar la configuracion de la interfaz de la tarjeta de red que usaremos para conectarnos. En mi caso es la <code>enp0s3</code>.</p> <p>Primero vamos a hacer un backup de nuevo archivo <code>ifcfg-enp0s3</code>, ejecutando el siguiente comando:</p> <pre><code>  cp ifcfg-enp0s3 ifcfg-enp0s3._backup\n</code></pre> <p>Para verificar que el backup fue creado correctamente ejecutamos el siguiente comando:</p> <pre><code>  ls -lah | grep ifcfg-enp0s3\n</code></pre> <p></p> <p>Ahora que tenemos el backup, vamos a editar el archivo <code>ifcfg-enp0s3</code> ejecutando:</p> <pre><code>  vi ifcfg-enp0s3\n</code></pre> <p>Actualiza las siguientes lineas:</p> <pre><code>  BOOTPROTO=static\n  IPADDR=192.168.10.11\n  IPV6ADDR=2001:db8:1::101\n</code></pre> <p>En este caso, asignamos <code>192.168.10.11</code> como IP est\u00e1tica y <code>2001:db8:1::101</code> como IPv6 est\u00e1tica.</p> <p>Ahora, vamos a configurar la puerta de enlace en el archivo <code>/etc/sysconfig/network</code>. Para hacerlo ejecutamos:</p> <pre><code>  cd /etc/sysconfig\n</code></pre> <p>Nos aseguramos de hacer un backup de nuestro archivo <code>network</code>, ejecutando:</p> <pre><code>  cp network network_backup\n</code></pre> <p>Para verificar que el backup fue creado correctamente ejecutamos el siguiente comando:</p> <pre><code>  ls -lah | grep network\n</code></pre> <p></p> <p>Ahora agreamos algunas lineas al archivo <code>network</code>:</p> <pre><code>  vi network\n</code></pre> <p>Debe ser un archivo vacio, agregamos las siguientes lineas:</p> <pre><code>  NETWORKING=yes\n  HOSTNAME=centos7\n  GATEWAY=192.168.10.1\n</code></pre> <p>Deberia quedar algo como esto:</p> <p></p> <p>El valor de <code>GATEWAY</code> es la puerta de enlace de tu maquina host. En este caso, es <code>192.168.10.1</code>.</p> <p>Ahora, vamos a reiniciar el servicio de red para que los cambios surtan efecto:</p> <pre><code>  systemctl restart network\n</code></pre>"},{"location":"tutorials/set-static-ip/#notas","title":"Notas","text":"<p>Para comprobar cual es la ip de tu maquina virtual, ejecuta:</p> <pre><code>  ip addr\n</code></pre> <p></p>"},{"location":"tutorials/setup/","title":"Configuracion inicial de CentOS 7.1","text":"<p>Una vez que tengas instalado CentOS 7.1 en tu maquina virtual o en tu maquina fisica, y tengas conexion a internet puedes instalar los siguientes paquetes para que sigas los tutoriales de esta pagina:</p> <p>Instalar <code>Vim</code> para editar archivos de texto:</p> <pre><code>yum -y install vim-enhanced\n</code></pre> <p>Instalar el servicio <code>DNS</code>:</p> <pre><code>yum -y install bind bind-chroot bind-utils bind-libs caching-nameserver\n</code></pre> <p>Instalar el servicio <code>Apache</code>:</p> <pre><code>yum -y install httpd php\n</code></pre> <p>Instalar el servicio <code>ssh</code>:</p> <pre><code>yum -y install openssh-server openssh-clients openssl\n</code></pre> <p>Instalar el servicio <code>telnet</code>:</p> <pre><code>yum -y install telnet\n</code></pre> <p>Instalar <code>netstat</code>:</p> <pre><code>yum -y install net-tools\n</code></pre>"},{"location":"tutorials/web-page/","title":"Despliegue de una pagina web en HTML","text":""},{"location":"tutorials/web-page/#despliegue-de-una-pagina-web-en-html","title":"Despliegue de una pagina web en HTML","text":"<p>Las piezas de software involucradas en la simple consulta de una pagina web pueden ser varias y complejas. En esta guia se describe el proceso de configuracion, en servidores locales o remotos, de los servicios esenciales para la visualizacion de una pagina web.</p>"},{"location":"tutorials/web-page/#configuracion-del-dns","title":"Configuracion del DNS","text":""},{"location":"tutorials/web-page/#configuracion-de-un-servidor-web-apache","title":"Configuracion de un servidor web Apache","text":""},{"location":"tutorials/web-page/#creacion-de-una-pagina-web-en-html","title":"Creacion de una pagina web en HTML","text":""},{"location":"tutorials/web-page/#transferencia-de-archivos-mediante-sftp","title":"Transferencia de archivos mediante SFTP","text":"<p>Una vez completados los pasos anteriores, se puede acceder a la pagina web desde cualquier navegador web utilizando el nombre de dominio asignado al servidor web.</p>"},{"location":"tutorials/web-page/#solucion-de-problemas","title":"Solucion de problemas","text":"<p>Si la pagina web no se muestra correctamente y obtiene un error similar a <code>Check if there is a typo in name.domain. DNS_PROBE_FINISHED_NXDOMAIN</code>, debe asegurarse que est\u00e1 utilizando el DNS configurado.</p>"}]}